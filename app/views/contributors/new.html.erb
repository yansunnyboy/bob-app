<div class="min-h-screen bg-gradient-to-t from-pink-200 to-purple-400 p-0  flex justify-center">
    <div class="max-w-sm rounded overflow-hidden shadow-md h-1/3 w-2/3 pt-3 ">
      <div class="px-6 py-4">
      <%= simple_form_for [@list, @contributor] do |f| %>
        <%= f.simple_fields_for :user do |p| %>
          <%= p.input :email%>
        <%end%>
        <%= f.submit "Invite", class:"bg-purple-700 hover:bg-purple-500 text-white font-bold py-2 px-4 rounded-full" %>
      <% end %>
    </div>
  </div>
</div>
