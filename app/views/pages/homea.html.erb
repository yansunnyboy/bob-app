<div class="min-h-screen bg-purple-300 p-20">
  <div>
    <button class="bg-pink-700 hover:bg-pink-300 text-white font-bold py-2 px-4 rounded">
    Button
  </button>
</div>
  <p class="font-mono txl-8xl"> Bob... Our Bussiness Buddy
</div>

<nav class= "fixed bottom-0 inset-x-0 bg bg-white py-4  flex justify-between h-20">
  <a href="../" class="w-full text-center "><i class="fas fa-home fa-2x text-pink-400 hover:text-purple-400"></i></a>
  <%= link_to(
    products_path,
    class: "w-full text-center "
    ) do %>
    <i class="fas fa-th-large fa-2x text-pink-400 hover:text-purple-400"></i>
    <% end %>
  <% if user_signed_in? %>
    <%= link_to(
        lists_path,
        class: "w-full text-center"
        )do %>
        <i class="fas fa-list-ul fa-2x text-pink-400 hover:text-purple-400"></i>
        <% end %>
    <div class = "w-full text-center">
      <i class="fas fa-user fa-2x text-pink-400 hover:text-purple-400" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"></i>
      <div class="dropdown-menu">
        <%= link_to "Edit your profile", edit_user_registration_path, class: "dropdown-item" %>
        <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
      </div>
    </div>
  <% else %>
    <%= link_to(
      new_user_session_path,
      class: "w-full text-center"
      ) do %>
      <i class="fas fa-list-ul fa-2x text-pink-400 hover:text-purple-400"></i>
      <% end %>
    <%= link_to(
    new_user_session_path,
    class: "w-full text-center"
    ) do %>
    <i class="fas fa-user fa-2x text-pink-400 hover:text-purple-400"></i>
    <% end %>
  <% end %>
</nav>

Others
UPVOTE ICON
<i class="fas fa-heart"></i>
ADD CONTRIBUTOR ICON in add contributors page
<i class="fas fa-user-plus"></i>
ADD ROUND ICON
<i class="fas fa-plus-circle"></i>
Cancel CROSS ICON
<i class="fas fa-times"></i>
<br></br>
