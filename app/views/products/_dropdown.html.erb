<%= simple_form_for [product, @solution || Solution.new], url: create_solution_from_product_product_path(product), method: 'post' do |f| %>
  <%= f.input :list, collection: @lists %>
  <%= f.submit 'Add to List' %>
<% end %>
<br>
<%= link_to 'Create a New List', new_list_path, class:'btn bg-pink-400 text-white' %>
