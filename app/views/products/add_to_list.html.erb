<%= simple_form_for [@product, @solution], url: create_solution_from_product_product_path, method: 'post' do |f| %>
  <%= f.input :list, collection: @lists %>
  <%= f.submit 'Add to List' %>
<% end %>
